# myDay -- Frontend

## Steps:

1. Download the project or clone it from the terminal.
2. Once that's done, cd into the `/client` directory. 
3. Run `npm install` to install all the required dependencies.
4. Finally, run `npm start` to start the development server. 

